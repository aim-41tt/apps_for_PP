spring:
  application:
    name: RestTaskService

  datasource:
    url: jdbc:postgresql://localhost:5440/task_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
